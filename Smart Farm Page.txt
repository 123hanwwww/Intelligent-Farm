<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Crop Monitoring System</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        :root {
            --primary: #4CAF50;
            --secondary: #388E3C;
            --warning: #FF9800;
            --danger: #F44336;
            --light: #F1F8E9;
            --dark: #1B5E20;
        }
        
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Segoe UI', sans-serif;
        }
        
        body {
            background-color: #f5f5f5;
            color: #333;
            line-height: 1.6;
        }
        
        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 20px;
        }
        
        header {
            background: linear-gradient(135deg, var(--primary), var(--dark));
            color: white;
            padding: 20px;
            border-radius: 8px;
            margin-bottom: 20px;
            text-align: center;
        }
        
        .dashboard {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 20px;
            margin-bottom: 20px;
        }
        
        .card {
            background: white;
            border-radius: 8px;
            box-shadow: 0 4px 6px rgba(0,0,0,0.1);
            padding: 20px;
            transition: transform 0.3s;
        }
        
        .card:hover {
            transform: translateY(-5px);
        }
        
        .card h2 {
            color: var(--dark);
            margin-bottom: 15px;
            padding-bottom: 10px;
            border-bottom: 2px solid var(--primary);
            display: flex;
            align-items: center;
        }
        
        .card h2 i {
            margin-right: 10px;
        }
        
        .status {
            display: flex;
            align-items: center;
            margin-bottom: 10px;
        }
        
        .dot {
            width: 12px;
            height: 12px;
            border-radius: 50%;
            margin-right: 10px;
        }
        
        .healthy { background: var(--primary); }
        .warning { background: var(--warning); }
        .danger { background: var(--danger); }
        
        .value {
            font-size: 1.8rem;
            font-weight: bold;
            color: var(--dark);
            margin: 10px 0;
            text-align: center;
            padding: 10px;
            background: var(--light);
            border-radius: 8px;
        }
        
        .data-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 10px;
            margin-top: 15px;
        }
        
        .data-item {
            padding: 8px;
            background: var(--light);
            border-radius: 4px;
            text-align: center;
        }
        
        .alert {
            background: var(--light);
            border-left: 5px solid var(--warning);
            padding: 15px;
            margin-bottom: 20px;
            border-radius: 8px;
            display: flex;
            align-items: center;
            box-shadow: 0 4px 6px rgba(0,0,0,0.1);
        }
        
        .alert i {
            font-size: 1.5rem;
            margin-right: 15px;
            color: var(--warning);
        }
        
        .camera {
            background: #000;
            border-radius: 8px;
            overflow: hidden;
            margin-bottom: 20px;
            box-shadow: 0 4px 6px rgba(0,0,0,0.1);
        }
        
        .camera-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 15px;
            background: rgba(0,0,0,0.7);
            color: white;
        }
        
        .camera-view {
            height: 300px;
            background: linear-gradient(45deg, #2E7D32, #4CAF50);
            display: flex;
            justify-content: center;
            align-items: center;
            color: white;
            position: relative;
        }
        
        .camera-content {
            text-align: center;
        }
        
        .camera-icon {
            font-size: 4rem;
            margin-bottom: 15px;
        }
        
        .hardware {
            display: flex;
            flex-wrap: wrap;
            gap: 15px;
            margin-top: 20px;
        }
        
        .hardware-item {
            flex: 1;
            min-width: 150px;
            text-align: center;
            padding: 15px;
            background: white;
            border-radius: 8px;
            box-shadow: 0 4px 6px rgba(0,0,0,0.1);
        }
        
        .controls {
            display: flex;
            justify-content: space-between;
            margin-top: 20px;
            flex-wrap: wrap;
            gap: 10px;
        }
        
        button {
            background: var(--primary);
            color: white;
            border: none;
            padding: 12px 24px;
            border-radius: 8px;
            cursor: pointer;
            font-weight: bold;
            display: flex;
            align-items: center;
            gap: 8px;
            flex: 1;
            min-width: 180px;
            justify-content: center;
        }
        
        button:hover {
            background: var(--secondary);
        }
        
        @media (max-width: 768px) {
            .dashboard {
                grid-template-columns: 1fr;
            }
            
            .controls {
                flex-direction: column;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <header>
            <h1><i class="fas fa-seedling"></i> Crop Monitoring System</h1>
            <p>Monitor crop health, weather, and pests in real-time</p>
        </header>
        
        <div class="alert">
            <i class="fas fa-exclamation-triangle"></i>
            <div>
                <h3>Camera Issue</h3>
                <p>Low clarity detected. This may affect analysis accuracy.</p>
            </div>
        </div>
        
        <div class="dashboard">
            <div class="card">
                <h2><i class="fas fa-heartbeat"></i> Crop Health</h2>
                <div class="status">
                    <div class="dot healthy"></div>
                    <span>Status: Good</span>
                </div>
                <div class="value">85/100</div>
                <div class="data-grid">
                    <div class="data-item">Chlorophyll: Normal</div>
                    <div class="data-item">Water: Adequate</div>
                    <div class="data-item">Growth: Flowering</div>
                    <div class="data-item">Nitrogen: Optimal</div>
                </div>
            </div>
            
            <div class="card">
                <h2><i class="fas fa-cloud-sun"></i> Weather</h2>
                <div class="status">
                    <div class="dot warning"></div>
                    <span>Cloudy, light rain</span>
                </div>
                <div class="data-grid">
                    <div class="data-item">Temp: 23°C</div>
                    <div class="data-item">Humidity: 65%</div>
                    <div class="data-item">Wind: 3.2 m/s</div>
                    <div class="data-item">Rain: 40%</div>
                </div>
            </div>
            
            <div class="card">
                <h2><i class="fas fa-bug"></i> Pest Detection</h2>
                <div class="status">
                    <div class="dot healthy"></div>
                    <span>Risk: Low</span>
                </div>
                <div class="data-grid">
                    <div class="data-item">Pests: None</div>
                    <div class="data-item">Threats: Aphids</div>
                    <div class="data-item">Last: 3 days</div>
                    <div class="data-item">Action: Monitor</div>
                </div>
            </div>
        </div>
        
        <div class="camera">
            <div class="camera-header">
                <h2><i class="fas fa-video"></i> Live Camera</h2>
                <div class="status">
                    <div class="dot warning"></div>
                    <span>Low Clarity</span>
                </div>
            </div>
            <div class="camera-view">
                <div class="camera-content">
                    <i class="fas fa-camera camera-icon"></i>
                    <h3>Camera Feed</h3>
                    <p>Image clarity may be insufficient</p>
                </div>
            </div>
        </div>
        
        <div class="hardware">
            <div class="hardware-item">
                <h3><i class="fas fa-camera"></i> Camera</h3>
                <div class="status">
                    <div class="dot warning"></div>
                    <span>Low Clarity</span>
                </div>
            </div>
            <div class="hardware-item">
                <h3><i class="fas fa-thermometer-half"></i> Thermometer</h3>
                <div class="status">
                    <div class="dot healthy"></div>
                    <span>Normal</span>
                </div>
            </div>
            <div class="hardware-item">
                <h3><i class="fas fa-tint"></i> Hygrometer</h3>
                <div class="status">
                    <div class="dot healthy"></div>
                    <span>Normal</span>
                </div>
            </div>
            <div class="hardware-item">
                <h3><i class="fas fa-desktop"></i> Display</h3>
                <div class="status">
                    <div class="dot healthy"></div>
                    <span>Normal</span>
                </div>
            </div>
        </div>
        
        <div class="controls">
            <button><i class="fas fa-sync-alt"></i> Refresh</button>
            <button><i class="fas fa-chart-bar"></i> Report</button>
            <button><i class="fas fa-cog"></i> Settings</button>
            <button><i class="fas fa-headset"></i> Support</button>
        </div>
    </div>

    <script>
        function updateData() {
            const healthValue = document.querySelector('.card .value');
            const currentScore = parseInt(healthValue.textContent);
            const newScore = Math.max(0, Math.min(100, currentScore + (Math.random() * 4 - 2)));
            healthValue.textContent = `${Math.round(newScore)}/100`;
            
            const healthDot = document.querySelector('.card:nth-child(1) .dot');
            if (newScore > 80) {
                healthDot.className = 'dot healthy';
            } else if (newScore > 60) {
                healthDot.className = 'dot warning';
            } else {
                healthDot.className = 'dot danger';
            }
            
            const tempElement = document.querySelector('.card:nth-child(2) .data-item:nth-child(1)');
            const currentTemp = parseInt(tempElement.textContent);
            const newTemp = currentTemp + (Math.random() * 2 - 1);
            tempElement.textContent = `Temp: ${newTemp.toFixed(1)}°C`;
        }
        
        setInterval(updateData, 10000);
        
        document.querySelectorAll('button')[0].addEventListener('click', updateData);
        document.querySelectorAll('button')[1].addEventListener('click', function() {
            alert('Generating report...');
        });
        document.querySelectorAll('button')[2].addEventListener('click', function() {
            alert('Opening settings');
        });
        document.querySelectorAll('button')[3].addEventListener('click', function() {
            alert('Contact support: support@agritech.com');
        });
    </script>
</body>
</html>
